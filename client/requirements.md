## Packages
recharts | For dashboard analytics charts
date-fns | For German date formatting
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
framer-motion | Smooth animations for dialogs and page transitions
clsx | Class name utilities
tailwind-merge | Class name merging

## Notes
Backend uses Replit Auth. 
PDF generation is server-side via /api/jobs/:id/pdf
Tailwind config should extend font family with "Plus Jakarta Sans" and "Inter"
